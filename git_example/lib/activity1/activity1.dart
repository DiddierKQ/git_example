import 'package:flutter/material.dart';

class Activity1Page extends StatelessWidget {
  const Activity1Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Activity 1");
  }
}